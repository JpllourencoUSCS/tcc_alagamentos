# Copie para ".env" (nunca commitado — está no .gitignore) e preencha com
# valores reais antes de "docker compose up -d".
POSTGRES_USER=alagamentos
POSTGRES_PASSWORD=troque-esta-senha
POSTGRES_DB=alagamentos
